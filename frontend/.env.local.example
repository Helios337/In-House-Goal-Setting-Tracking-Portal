# Native dev (API on host port 4000 per root .env API_PORT)
API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=uGrdCemXQVeoP+ho0YFcWynmkneV9H5Riyn42RNFVVk=

# Azure AD (NextAuth provider — optional if using password login only)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# Must match JWT_SECRET on the API in production (≥ 32 bytes)
# NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32

# Set to "true" to show demo role picker and pre-filled credentials in production builds
# NEXT_PUBLIC_ENABLE_DEMO_LOGIN=false
